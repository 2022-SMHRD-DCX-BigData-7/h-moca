<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hmoca.mapper.CmtMapper">
	  
	  
	 
	  <insert id ="insertComment" parameterType="com.hmoca.entity.Comment">
	  	insert into comment_info (cmt_content, created_at, user_id, post_idx) 
	  	values (#{cmt_content}, sysdate, #{user_id}, #{post_idx})
	  </insert>
	  
	  <select id="commentList" resultType="com.hmoca.entity.Comment"> 
	    select * from comment_info order by cmt_idx desc
	</select>
	
	  	


</mapper>